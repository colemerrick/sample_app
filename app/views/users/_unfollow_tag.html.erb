<%= form_for(current_user.tag_relationships.find_by(followed_tag_id: @tag),
             html: { method: :delete }, remote: true) do |f| %>
  <%= f.submit "Unfollow Tag", class: "btn btn-large" %>
<% end %>